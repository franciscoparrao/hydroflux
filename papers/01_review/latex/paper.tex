%% hydroflux methods paper for Environmental Modelling & Software (EMS)
%% Using elsarticle with Harvard (author-year) bibliography style.
%%
%% Compile with xelatex (native unicode in monospace + math fonts):
%%   xelatex paper && bibtex paper && xelatex paper && xelatex paper
%%
\documentclass[preprint,review,12pt,authoryear]{elsarticle}

%% --- Font / unicode setup for xelatex ---
\usepackage{fontspec}
\setmainfont{Latin Modern Roman}
\setsansfont{Latin Modern Sans}
\setmonofont[Scale=0.95]{DejaVu Sans Mono}
\usepackage{unicode-math}
\setmathfont{Latin Modern Math}

%% --- Math, tables, references, code ---
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{calc}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{array}
% pandoc helpers for longtable column widths
\newlength{\hfdcw}
\setlength{\hfdcw}{\columnwidth}
\providecommand{\real}[1]{#1}
\usepackage{lineno}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{siunitx}

\journal{Environmental Modelling \& Software}

\begin{document}

\begin{frontmatter}

\title{hydroflux: A Well-Balanced, Differentiable-by-Design 2D
Shallow-Water Solver in Rust, Verified Against Analytical and Community
Benchmarks and Applied to a Semiarid Andean Reach}

\author[usach]{Francisco Parra\corref{cor1}}
\ead{francisco.parra.o@usach.cl}
\cortext[cor1]{Corresponding author}

\author[unsl]{Verónica Gil-Costa}
\ead{gvcosta@unsl.edu.ar}

\author[usach]{Carolina Bonacic}
\ead{carolina.bonacic@usach.cl}

\author[usach]{Mauricio Marín}
\ead{mauricio.marin@usach.cl}

\affiliation[usach]{organization={Departamento de Ingeniería Informática,
Universidad de Santiago de Chile},%
            city={Santiago},%
            country={Chile}}

\affiliation[unsl]{organization={Universidad Nacional de San Luis (UNSL)
and CONICET},%
            city={San Luis},%
            country={Argentina}}

\begin{abstract}
No production shallow-water flood solver ships with automatic
differentiation. We present \emph{hydroflux},
a finite-volume solver in Rust generic over its numeric type, so the
identical code evaluates in \texttt{f64} for production and in forward-mode
dual numbers for gradients. It is well-balanced and mass-conservative at wet/dry fronts, matches
published series on two official Environment Agency benchmarks to
0.3--1.2 \% RMSE, and reproduces an independent GPU solver to 0.021 m
RMSE on a semiarid Andean reach.
Building it produced two results that generalise past it. Over a
simulated day the tangent linear model grows 1.85 \% per step while the
primal stays stable, confining gradients to short assimilation windows;
the bound belongs to the linearisation, so an adjoint inherits it. And
two solvers given the same boundary hydrograph delivered volumes
differing by 8.4 \%, which accounted for almost all of an apparent
disagreement between their depth fields. Solver and tests are open.
\end{abstract}

\begin{highlights}
\item A 2D shallow-water solver in Rust, generic over the numeric type for f64 + AD.
\item Gradients verified layer-by-layer against finite differences; overhead 2.01$\times$.
\item The tangent diverges at 1.85 \%/step while the primal is stable; adjoints inherit it.
\item Verified on Thacker, Stoker, MacDonald and two official UK EA tests.
\item Cross-validated vs ANUGA and vs SynxFlow on real terrain: 0.021 m RMSE, CSI 0.950.
\end{highlights}

\begin{keyword}
shallow-water equations \sep flood modelling \sep finite volume \sep
automatic differentiation \sep Rust \sep well-balanced schemes
\end{keyword}

\end{frontmatter}

\linenumbers

\input{body_clean}

\input{figures}

\section*{Author Contributions}

CRediT roles \citep{Brand2015}:

\begin{itemize}
\item \textbf{Francisco Parra} --- Conceptualization; Methodology;
  Software (solver-2d, autograd, verification suite, application
  pipeline); Validation; Formal analysis; Investigation; Data
  curation; Writing --- original draft; Writing --- review \&
  editing; Visualization; Project administration; Funding acquisition
  (DICYT postdoctoral fellowship).
\item \textbf{Verónica Gil-Costa} --- Methodology (numerical schemes
  and benchmarking); Validation; Writing --- review \& editing;
  Supervision (computational methodology).
\item \textbf{Carolina Bonacic} --- Software (high-performance
  computing and parallelism review); Validation; Writing --- review
  \& editing.
\item \textbf{Mauricio Marín} --- Conceptualization (research line
  and venue positioning); Methodology (software-engineering rigor
  and reproducibility); Writing --- review \& editing; Supervision
  (Rust systems programming and software engineering); Resources
  (computing infrastructure).
\end{itemize}

All authors read and approved the final manuscript.

\section*{Open Research}

All code is released open-source at \url{https://github.com/franciscoparrao/hydroflux}
(commit \texttt{2fb4f1b}), dual-licensed MIT OR Apache-2.0. The
$solver-2d$ crate contains the finite-volume solver ($state$, $flux$,
$riemann$, $geometry$, \texttt{boundary}, $update$, $source$, $io$ modules)
and its verification suite (the \texttt{report\_*} ignored tests print the \S3
metrics; the workspace runs 307 automated tests at the pinned commit,
0 failures). The Huasco application is
reproduced by the \texttt{huasco\_2d\_event} and \texttt{huasco\_2d\_event\_landcover}
examples. The repository is self-contained: its single
geospatial dependency --- SurtGIS \citep{SurtgisRef}, a raster I/O crate
written by the first author and described in a companion paper in this
journal --- is pinned by commit in the build manifest, and the 30 m Huasco subset
rasters (DEM, flow accumulation, land cover; $\sim$80 kB total) ship with
the repository, so the commands below work from a fresh clone with a
stock Rust toolchain (Rust $\geq$ 1.85, edition 2024) and require no manual
dependency or data setup. The script that regenerates the subsets from
the public SRTM and ESA WorldCover sources is included
(\texttt{extract\_subset.py}). Continuous
integration (GitHub Actions) runs the full release-mode verification
suite plus an analytical-benchmark gate that fails the build on any
regression of the \S3 criteria; the numerical core forbids unsafe code
(\texttt{\#![forbid(unsafe\_code)]}). Build and verify:

\begin{verbatim}
cargo test --release --workspace             # 307 tests, 0 failures
cargo run --release -p hydroflux-solver-2d --example huasco_2d_event_landcover -- --days 1
cargo run --release -p hydroflux-solver-2d --example huasco_manning_sweep   # Table 3
cargo run --release -p hydroflux-solver-2d --example m1_forward_scaling     # \S2.5 scaling
cargo run --release -p hydroflux-solver-2d --example export_huasco_inputs   # \S4.5 hand-off
cargo run --release -p hydroflux-solver-2d --example huasco_closed_domain -- --no-inflow
\end{verbatim}

DGA streamflow data are public via the CR2 archive
(\url{https://www.cr2.cl/}). The DEM is SRTM 30 m (USGS) pit-filled with
the SurtGIS pipeline \citep{SurtgisRef}; the land cover is ESA WorldCover
2021 v200 \citep{ESAWorldCover2021}.

\section*{Declaration of competing interest}

The author declares no competing financial or personal interests.

\section*{Data availability}

All code, all benchmark configurations and every input required to
reproduce the figures and tables are public at the repository and
pinned commit given in Open Research, under a dual MIT OR Apache-2.0
licence. The Huasco subset rasters (DEM, flow accumulation, land cover)
and the reference depth field used for the \S4.5 cross-validation ship
with the repository, so no separate download is needed; the script that
regenerates the subsets from the public SRTM and ESA WorldCover sources
is included. The third-party inputs are cited at their own DOIs: the
UK Environment Agency benchmark geometry as redistributed by
\citep{Shaw2021} and \citep{Sharifian2023}, and ESA WorldCover
\citep{ESAWorldCover2021}. The Río Huasco discharge record is public through
the CR2 archive (\url{https://www.cr2.cl/}), station 03820003. An archived
snapshot of the repository with its own DOI will be deposited at
acceptance and cited here in proof.

\section*{Acknowledgements}

This work is part of the DICYT postdoctoral fellowship 2026--2027 at
Universidad de Santiago de Chile, held by F.P. under the sponsorship of
M.M.

\section*{Declaration on generative AI use}

Generative AI tools were used to assist drafting, refactoring, and
reference verification. All content was reviewed and validated by the
author. Three references in an earlier draft (Hydrograd.jl, AegirJAX,
r.avaflow v4) failed verify-refs / OpenAlex checks and were removed
before submission.

\bibliographystyle{elsarticle-harv}
\bibliography{references}

\end{document}
